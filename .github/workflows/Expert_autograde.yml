name: Auto-grading Code Écolo - Niveau Expert

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"
      - name: Run tests
        run: |
          python -m Exercices_Expert.tests.test_plus_longue_sous_sequence
          python -m Exercices_Expert.tests.test_max_somme_contigue
