name: 🌱 Éco-Coding - Niveau Débutant

on:
  push:
    paths:
      - "Exercices_Debutant/**"
  pull_request:
    paths:
      - "Exercices_Debutant/**"

permissions:
  contents: write

jobs:
  test-debutant:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Configuration Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"

      - name: Exécution des tests Débutant
        run: |
          python -m Exercices_Debutant.tests.test_judge

      - name: Commit du rapport Débutant
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Bot Éco-Coding"
          if [ -f ECO_REPORT_DEBUTANT.md ]; then
            git add ECO_REPORT_DEBUTANT.md
            git diff --quiet && git diff --staged --quiet || git commit -m "📊 Mise à jour Rapport Débutant [skip ci]"
            git push
          else
            echo "⚠️ ECO_REPORT_DEBUTANT.md not found, skipping commit"
          fi
