name: 🌱 Éco-Coding - Niveau Débutant

on:
  push:
    paths:
      - "Exercices_Debutant/**"
  pull_request:
    paths:
      - "Exercices_Debutant/**"

permissions:
  contents: write

jobs:
  test-debutant:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Configuration Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"

      - name: Exécution des tests Débutant
        run: |
          python -m Exercices_Debutant.tests.test_judge
        continue-on-error: true
      - name: Commit du rapport
        if: always()
        run: |
          REPORT_FILE="ECO_REPORT_DEBUTANT.md"
          if [ -f "$REPORT_FILE" ]; then
            git config --local user.email "action@github.com"
            git config --local user.name "Bot Éco-Coding"
            git add "$REPORT_FILE"
            # Commit only if there are changes
            git diff --quiet && git diff --staged --quiet || git commit -m "💎 Mise à jour $REPORT_FILE [skip ci]"
            # Push safely to current branch (force if needed)
            CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
            git push origin "$CURRENT_BRANCH" --force
          else
            echo "⚠️ $REPORT_FILE not found, skipping commit"
          fi
