name: 🚀 Éco-Coding - Niveau Avancé

on:
  push:
    paths:
      - "Exercices_Avance/**"
  pull_request:
    paths:
      - "Exercices_Avance/**"

permissions:
  contents: write

jobs:
  test-avance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Configuration Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"

      - name: Exécution des tests Avancé
        run: |
          python test_judge_avance.py

      - name: Commit du rapport Avancé
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Bot Éco-Coding"
          if [ -f ECO_REPORT_AVANCE.md ]; then
            git add ECO_REPORT_AVANCE.md
            git diff --quiet && git diff --staged --quiet || git commit -m "🚀 Mise à jour Rapport Avancé [skip ci]"
            git push
          else
            echo "⚠️ ECO_REPORT_AVANCE.md not found, skipping commit"
          fi
